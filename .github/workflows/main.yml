name: Make-build

on:
  - push
  - pull_request

jobs:
  test-make-build:
    runs-on: ubuntu-latest

    steps:
      - name: Git clone my repo
      - uses: actions/checkout@v2
      
      - name: Build with Gradle
        run: make build 
